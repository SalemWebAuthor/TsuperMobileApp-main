name: homepage
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'  # Updated to match current Dart SDK versioning conventions

dependencies:
  flutter_osm_plugin: any
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.0.6
  latlng: ^2.0.5
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  cupertino_icons: ^1.0.2
  qr_code_scanner: ^1.0.0
  qr_flutter: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/jeeplogo.png
  uses-material-design: true
